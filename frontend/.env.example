# Copiar para .env.production e preencher antes de publicar no Vercel (ou
# definir diretamente nas variáveis de ambiente do projeto no dashboard).
# Em desenvolvimento local não é preciso — o Vite faz proxy para
# localhost:8000 automaticamente (ver vite.config.ts).

# URL pública do backend publicado (Render), sem barra final.
VITE_API_BASE_URL=https://mentores-espirituais-backend.onrender.com
